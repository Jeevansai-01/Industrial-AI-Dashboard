# Environment Variables for the Industrial AI Dashboard
#
# To use this file, copy it to a new file named .env and provide
# the appropriate values for your local environment.
#
# The .gitignore file is configured to ignore the actual .env file,
# so your local settings will not be committed to version control.

# Path to the SQLite database file.
# It's recommended to keep this in a 'data' directory.
DB_PATH=data/sensor_data.db

# Flask specific settings
# Use 'development' for local testing to enable debug mode and auto-reloading.
# Use 'production' when deploying the application.
FLASK_ENV=development
